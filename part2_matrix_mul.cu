#include <stdio.h>
#include <stdlib.h>
#include <cuda.h>



// allocate memory on CPU and GPU

//generate random matrices M and N

//copy matrices to GPU

//launching CUDA kernel to perform matrix multiplication on the GPU

//copying the result back on the CPU

//computing the same matrix multiplication on the CPU

//comparing the CPU and GPU results

//measuring and analyzing data transfer overhead

//freeing GPU memory